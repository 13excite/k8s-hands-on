apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: custom-proxy-config
  namespace: basic-gw
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyService: 
        type: NodePort
